# Flymail Docker Configuration

# JWT secret key (change this!)
JWT_SECRET=your-super-secret-jwt-key-change-this

# MX hostname (your mail server domain)
MX_HOSTNAME=mail.yourdomain.com

# Ports (change if needed)
WEB_PORT=8080
API_PORT=3001
SMTP_PORT=25
